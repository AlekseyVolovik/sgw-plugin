<div class="sgw-no-matches">
    <div class="sgw-no-matches__info">
        <div class="sgw-no-matches__icon">⚽</div>
        {% if no_matches_card.title %}
            <div class="sgw-no-matches__title">{{ no_matches_card.title }}</div>
        {% endif %}
        {% if no_matches_card.subtitle %}
            <p class="sgw-no-matches__subtitle">{{ no_matches_card.subtitle }}</p>
        {% endif %}
    </div>
</div>